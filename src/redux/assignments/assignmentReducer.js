import reducer from "../user/userReducer";

const initialState =  {
    assignments : [],
    submittedAssignments : []
};

const reducer = (state=initialState, action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state;
    }
}